# Copy to .env (gitignored) and fill in. Never commit real values.

# HS256 signing secret for JWTs — generate with:  openssl rand -hex 64
JWT_SECRET=

# Local Postgres password (dev only)
POSTGRES_PASSWORD=taskflow
